# Secretos de docker-compose. Copia este archivo a `.env` (gitignored) y cambia los valores.
# docker compose carga `.env` automáticamente desde este directorio.
POSTGRES_PASSWORD=psychosim_secret
DB_PASSWORD=psychosim_secret
JWT_SECRET=cambia-esto-por-un-secreto-fuerte-de-al-menos-256-bits
