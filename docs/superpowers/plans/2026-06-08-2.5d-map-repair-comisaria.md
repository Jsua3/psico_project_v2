# 2.5D Map Repair — comisaria-sala-espera Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar `comisaria-sala-espera.json` con un mapa Tiled completo de 7 capas 2.5D (floor/walls/props_back/props_front/lighting/overlay/Objects) generado por un script Python, y corregir el bug de ordenación depth-0 en `renderRoom()`.

**Architecture:** Un script Python autocontenido genera el JSON directamente con `firstgid` hardcodeados para tiny-dungeon (1) y rpg-urban (133). Los tests Python validan la estructura del JSON generado. El fix TypeScript es una cirugía de 2 ediciones en `renderRoom()`: insertar el rect procedural antes del try-block y eliminarlo de su posición actual (post try-block), de modo que los floor tiles (depth 0, insertados por `buildTiledLayers`) queden sobre el rect (también depth 0, insertado antes = debajo en Phaser 3).

**Tech Stack:** Python 3 (stdlib only), Angular 21 / Phaser 3 / TypeScript 5, Tiled JSON v1.10.

---

## File Map

| Acción | Archivo | Responsabilidad |
|---|---|---|
| Crear | `tools/map-generator/generate_comisaria_sala_espera.py` | Genera el JSON con las 7 capas 2.5D |
| Crear | `tools/map-generator/test_generated_map.py` | Tests estructurales del JSON generado |
| Reemplazar (auto) | `frontend/src/assets/game/maps/comisaria-sala-espera.json` | Salida del script — nuevo mapa 2.5D |
| Modificar | `frontend/src/app/features/simulator/game-world.component.ts` | Fix orden rect procedural en `renderRoom()` |

---

## Task 1: Tests estructurales del JSON (TDD — fase roja)

**Files:**
- Create: `tools/map-generator/test_generated_map.py`

Los tests validan la estructura y el contenido del JSON que el generador producirá. En este punto, el JSON no existe todavía: todos los tests fallarán.

- [ ] **Paso 1.1: Crear el directorio y el archivo de tests**

```bash
mkdir tools\map-generator
```

Crear `tools/map-generator/test_generated_map.py` con el contenido completo:

```python
"""
Tests estructurales para comisaria-sala-espera.json.
Ejecutar: python tools/map-generator/test_generated_map.py
(También compatible con pytest si está instalado.)
"""
import json
import sys
from pathlib import Path

MAP_PATH = Path("frontend/src/assets/game/maps/comisaria-sala-espera.json")
MAP_W, MAP_H = 60, 33


def _load():
    return json.loads(MAP_PATH.read_text(encoding="utf-8"))


def _layer(data, name):
    return next((l for l in data["layers"] if l["name"] == name), None)


def test_map_file_exists():
    assert MAP_PATH.exists(), f"Archivo no encontrado: {MAP_PATH}"


def test_dimensions_60x33():
    d = _load()
    assert d["width"] == MAP_W and d["height"] == MAP_H, \
        f"Esperado {MAP_W}×{MAP_H}, obtenido {d['width']}×{d['height']}"


def test_required_2_5d_layers_present():
    names = {l["name"] for l in _load()["layers"]}
    for req in ("floor", "walls", "props_back", "props_front", "Objects"):
        assert req in names, f"Capa requerida ausente: {req}"


def test_floor_has_tiles():
    layer = _layer(_load(), "floor")
    assert layer is not None, "Capa floor no encontrada"
    nonzero = sum(1 for g in layer["data"] if g != 0)
    assert nonzero > 1000, f"floor tiene {nonzero} tiles — esperado > 1000"


def test_props_back_has_furniture():
    layer = _layer(_load(), "props_back")
    assert layer is not None, "Capa props_back no encontrada"
    nonzero = sum(1 for g in layer["data"] if g != 0)
    assert nonzero > 0, "props_back está vacío — sin mobiliario"


def test_props_front_has_counter():
    layer = _layer(_load(), "props_front")
    assert layer is not None, "Capa props_front no encontrada"
    nonzero = sum(1 for g in layer["data"] if g != 0)
    # rows 19-21 × cols 8-36 = ~63 tiles (brazo L + superficie + frente)
    assert nonzero >= 58, f"props_front tiene {nonzero} tiles — esperado ≥ 58 (mostrador rows 19-21, cols 8-36)"


def test_objects_layer_preserved():
    obj_layer = _layer(_load(), "Objects")
    assert obj_layer is not None, "Capa Objects no encontrada"
    names = {o["name"] for o in obj_layer["objects"]}
    for expected in ("exit-to-consultorio", "familiar-comisaria", "colega-comisaria"):
        assert expected in names, f"Objeto faltante: {expected}"


def test_tilesets_include_both():
    ts_names = {t["name"] for t in _load()["tilesets"]}
    assert "tiny-dungeon" in ts_names, "tiny-dungeon ausente de tilesets"
    assert "rpg-urban" in ts_names, "rpg-urban ausente de tilesets"


def test_rpg_urban_firstgid_is_133():
    tilesets = _load()["tilesets"]
    ru = next((t for t in tilesets if t["name"] == "rpg-urban"), None)
    assert ru is not None, "tileset rpg-urban no encontrado"
    assert ru["firstgid"] == 133, f"rpg-urban firstgid={ru['firstgid']}, esperado 133"


def test_walls_border_top_row_closed():
    layer = _layer(_load(), "walls")
    assert layer is not None, "Capa walls no encontrada"
    top_row = layer["data"][:MAP_W]
    assert all(g != 0 for g in top_row), "Fila norte de paredes tiene celdas vacías"


def test_walls_border_bottom_row_closed():
    layer = _layer(_load(), "walls")
    bot_row = layer["data"][(MAP_H - 1) * MAP_W:]
    assert all(g != 0 for g in bot_row), "Fila sur de paredes tiene celdas vacías"


def test_door_gap_on_right_wall_rows_14_to_16():
    """El hueco de la puerta (exit-to-consultorio, col 59, filas 14-16) debe ser GID 0."""
    layer = _layer(_load(), "walls")
    for r in (14, 15, 16):
        idx = r * MAP_W + (MAP_W - 1)  # col 59
        assert layer["data"][idx] == 0, f"Fila {r} col 59 debería ser 0 (hueco de puerta), es {layer['data'][idx]}"


# ── Runner autónomo ────────────────────────────────────────────────────────────
if __name__ == "__main__":
    tests = sorted((n, f) for n, f in globals().items() if n.startswith("test_"))
    failed = 0
    for name, fn in tests:
        try:
            fn()
            print(f"PASS  {name}")
        except Exception as e:
            print(f"FAIL  {name}: {e}")
            failed += 1
    print(f"\n{'✓ Todos los tests pasaron' if not failed else f'✗ {failed} test(s) fallaron'}")
    sys.exit(failed)
```

- [ ] **Paso 1.2: Ejecutar tests para confirmar fase roja**

```bash
python tools/map-generator/test_generated_map.py
```

Resultado esperado: `FAIL  test_map_file_exists: Archivo no encontrado: frontend/src/assets/game/maps/comisaria-sala-espera.json` (todos los tests fallan porque el JSON no existe aún).

---

## Task 2: Script generador Python

**Files:**
- Create: `tools/map-generator/generate_comisaria_sala_espera.py`

El script genera el JSON completo con 7 capas. Es idempotente: cada ejecución sobreescribe el archivo.

- [ ] **Paso 2.1: Crear el script generador**

Crear `tools/map-generator/generate_comisaria_sala_espera.py`:

```python
"""
Generador: comisaria-sala-espera.json
Produce un mapa Tiled 2.5D completo (60×33 tiles, 16px/tile) con las capas:
  floor, walls, props_back, props_front, lighting (vacía), overlay (vacía), Objects.

Ejecutar desde la raíz del proyecto:
    python tools/map-generator/generate_comisaria_sala_espera.py

GIDs marcados VALIDAR: comprobar visualmente en el navegador.
Si un tile se ve incorrecto, ajustar la constante y re-ejecutar (idempotente).
"""
import json
from pathlib import Path

# ── Tile ID constants ──────────────────────────────────────────────────────────
# tiny-dungeon: firstgid=1, 12 cols.   GID = 1 + (row-1)*12 + (col-1)
TD_WALL      = 1    # (row=1, col=1) — stone wall, ya en uso en mapa original

# rpg-urban: firstgid=133, 27 cols.  GID = 133 + (row-1)*27 + (col-1)
def _ru(row1: int, col1: int) -> int:
    """GID rpg-urban por fila/columna 1-indexadas."""
    return 133 + (row1 - 1) * 27 + (col1 - 1)

RU_FLOOR_GRAY    = _ru(1, 4)   # VALIDAR — suelo gris interior
RU_FLOOR_GRAY2   = _ru(1, 5)   # VALIDAR — variante suelo (chequerboard)
RU_CABINET_TOP   = _ru(4, 1)   # VALIDAR — parte superior fichero
RU_CABINET_MID   = _ru(5, 1)   # VALIDAR — parte media fichero
RU_CHAIR         = _ru(4, 7)   # VALIDAR — silla de espera
RU_TABLE         = _ru(4, 9)   # VALIDAR — mesa pequeña
RU_COUNTER_TOP   = _ru(6, 4)   # VALIDAR — superficie del mostrador
RU_COUNTER_FRONT = _ru(7, 4)   # VALIDAR — frente del mostrador

MAP_W, MAP_H = 60, 33

# ── Funciones de pintura por capa ──────────────────────────────────────────────

def _paint_floor(r: int, c: int) -> int:
    """Interior (rows 1-31, cols 1-58): chequerboard. Borde → 0."""
    if r == 0 or r == MAP_H - 1 or c == 0 or c == MAP_W - 1:
        return 0
    return RU_FLOOR_GRAY if (r + c) % 2 == 0 else RU_FLOOR_GRAY2


def _paint_walls(r: int, c: int) -> int:
    """Borde de 1 tile. Hueco en col 59, filas 14-16 (exit-to-consultorio)."""
    on_border = (r == 0 or r == MAP_H - 1 or c == 0 or c == MAP_W - 1)
    if not on_border:
        return 0
    if c == MAP_W - 1 and 14 <= r <= 16:   # hueco de puerta
        return 0
    return TD_WALL


def _paint_props_back(r: int, c: int) -> int:
    """Mobiliario norte: ficheros NW/NE, sillas de espera E/W, mesas centrales."""
    # Ficheros NW: rows 1-3, cols 1-4
    if r == 1 and 1 <= c <= 4:
        return RU_CABINET_TOP
    if 2 <= r <= 3 and 1 <= c <= 4:
        return RU_CABINET_MID
    # Ficheros NE: rows 1-3, cols 55-58
    if r == 1 and 55 <= c <= 58:
        return RU_CABINET_TOP
    if 2 <= r <= 3 and 55 <= c <= 58:
        return RU_CABINET_MID
    # Sillas W: rows 5 y 7, cols 2, 5, 8, 11
    if r in (5, 7) and c in (2, 5, 8, 11):
        return RU_CHAIR
    # Sillas E: rows 5 y 7, cols 47, 50, 53, 56
    if r in (5, 7) and c in (47, 50, 53, 56):
        return RU_CHAIR
    # Mesas centrales: rows 4-5, cols 24-28 y 31-35
    if 4 <= r <= 5 and 24 <= c <= 28:
        return RU_TABLE
    if 4 <= r <= 5 and 31 <= c <= 35:
        return RU_TABLE
    return 0


def _paint_props_front(r: int, c: int) -> int:
    """Mostrador de recepción. Brazo L (rows 19-20, cols 8-9), cuerpo (rows 20-21, cols 8-36)."""
    # Brazo L y borde superior-izquierdo: rows 19-20, cols 8-9
    if r in (19, 20) and c in (8, 9):
        return RU_COUNTER_FRONT
    # Superficie del mostrador: row 20, cols 8-36 (nota: cols 8-9 ya asignados arriba → retorna FRONT)
    if r == 20 and 8 <= c <= 36:
        return RU_COUNTER_TOP
    # Frente del mostrador: row 21, cols 8-36
    if r == 21 and 8 <= c <= 36:
        return RU_COUNTER_FRONT
    return 0

# ── Construcción de capas ──────────────────────────────────────────────────────

def _build_data(paint_fn) -> list[int]:
    """Genera el array plano de GIDs (row-major) para MAP_W × MAP_H."""
    return [paint_fn(r, c) for r in range(MAP_H) for c in range(MAP_W)]


def _tilelayer(name: str, paint_fn, layer_id: int) -> dict:
    return {
        "data": _build_data(paint_fn),
        "height": MAP_H,
        "id": layer_id,
        "name": name,
        "opacity": 1,
        "type": "tilelayer",
        "visible": True,
        "width": MAP_W,
        "x": 0,
        "y": 0,
    }


def _empty_layer(name: str, layer_id: int) -> dict:
    return _tilelayer(name, lambda r, c: 0, layer_id)


# ── Capa Objects (preservada del mapa original) ────────────────────────────────
_OBJECTS_LAYER = {
    "draworder": "topdown",
    "id": 7,
    "name": "Objects",
    "objects": [
        {
            "height": 48, "id": 1, "name": "exit-to-consultorio",
            "rotation": 0, "type": "EXIT", "visible": True, "width": 32,
            "x": 920, "y": 240,
        },
        {
            "height": 16, "id": 2, "name": "familiar-comisaria",
            "rotation": 0, "type": "NPC", "visible": True, "width": 16,
            "x": 288, "y": 176,
        },
        {
            "height": 16, "id": 3, "name": "colega-comisaria",
            "rotation": 0, "type": "NPC", "visible": True, "width": 16,
            "x": 416, "y": 240,
        },
    ],
    "opacity": 1,
    "type": "objectgroup",
    "visible": True,
    "x": 0,
    "y": 0,
}

# ── Tilesets ───────────────────────────────────────────────────────────────────
_TILESETS = [
    {
        "columns": 12,
        "firstgid": 1,
        "image": "../kenney/tiny-dungeon/Tilemap/tilemap_packed.png",
        "imageheight": 176,
        "imagewidth": 192,
        "margin": 0,
        "name": "tiny-dungeon",
        "spacing": 0,
        "tilecount": 132,
        "tileheight": 16,
        "tilewidth": 16,
    },
    {
        "columns": 27,
        "firstgid": 133,
        "image": "../kenney/rpg-urban-pack/Spritesheet/tilemap_packed.png",
        "imageheight": 288,
        "imagewidth": 432,
        "margin": 0,
        "name": "rpg-urban",
        "spacing": 0,
        "tilecount": 486,
        "tileheight": 16,
        "tilewidth": 16,
    },
]

# ── Mapa completo ──────────────────────────────────────────────────────────────
_MAP = {
    "compressionlevel": -1,
    "height": MAP_H,
    "infinite": False,
    "layers": [
        _tilelayer("floor",       _paint_floor,       1),
        _tilelayer("walls",       _paint_walls,       2),
        _tilelayer("props_back",  _paint_props_back,  3),
        _tilelayer("props_front", _paint_props_front, 6),
        _empty_layer("lighting",  4),
        _empty_layer("overlay",   5),
        _OBJECTS_LAYER,
    ],
    "nextlayerid": 8,
    "nextobjectid": 10,
    "orientation": "orthogonal",
    "renderorder": "right-down",
    "tiledversion": "1.12.2",
    "tileheight": 16,
    "tilesets": _TILESETS,
    "tilewidth": 16,
    "type": "map",
    "version": "1.10",
    "width": MAP_W,
}

if __name__ == "__main__":
    out = Path("frontend/src/assets/game/maps/comisaria-sala-espera.json")
    out.write_text(json.dumps(_MAP, separators=(",", ":")), encoding="utf-8")
    print(f"Generado: {out}")
    for layer in _MAP["layers"]:
        if layer["type"] == "tilelayer":
            n = sum(1 for g in layer["data"] if g != 0)
            print(f"  {layer['name']:16s} {n:5d} tiles no-cero")
        else:
            print(f"  {layer['name']:16s} {len(layer.get('objects', []))} objetos")
```

---

## Task 3: Ejecutar el generador y pasar los tests (TDD — fase verde)

**Files:**
- Modify (auto): `frontend/src/assets/game/maps/comisaria-sala-espera.json`

- [ ] **Paso 3.1: Ejecutar el script generador**

```bash
python tools/map-generator/generate_comisaria_sala_espera.py
```

Salida esperada:
```
Generado: frontend/src/assets/game/maps/comisaria-sala-espera.json
  floor             1798 tiles no-cero
  walls              193 tiles no-cero
  props_back          60 tiles no-cero
  props_front         63 tiles no-cero
  lighting             0 tiles no-cero
  overlay              0 tiles no-cero
  Objects          3 objetos
```

_(Los conteos exactos pueden diferir ligeramente si se ajustan las constantes GID.)_

- [ ] **Paso 3.2: Ejecutar los tests estructurales (deben pasar)**

```bash
python tools/map-generator/test_generated_map.py
```

Resultado esperado: todos los tests con `PASS`. Si alguno falla:
- `test_floor_has_tiles`: el `paint_floor` no cubre el interior — revisar condiciones de borde.
- `test_props_front_has_counter`: revisar rangos de `_paint_props_front`.
- `test_door_gap_on_right_wall_rows_14_to_16`: revisar lógica del hueco en `_paint_walls`.

- [ ] **Paso 3.3: Verificar el JSON generado a alto nivel**

```bash
python -c "import json; d=json.load(open('frontend/src/assets/game/maps/comisaria-sala-espera.json')); print('Capas:', [l['name'] for l in d['layers']]); print('Tilesets:', [t['name'] for t in d['tilesets']])"
```

Salida esperada:
```
Capas: ['floor', 'walls', 'props_back', 'props_front', 'lighting', 'overlay', 'Objects']
Tilesets: ['tiny-dungeon', 'rpg-urban']
```

- [ ] **Paso 3.4: Commit — generador + tests + JSON**

```bash
git add tools/map-generator/generate_comisaria_sala_espera.py
git add tools/map-generator/test_generated_map.py
git add frontend/src/assets/game/maps/comisaria-sala-espera.json
git commit -m "feat(maps): add 2.5D layer generator for comisaria-sala-espera

Script generates floor/walls/props_back/props_front layers with rpg-urban
and tiny-dungeon tiles. Objects layer preserved from original.
Structural tests validate layer count, tile presence, and border integrity."
```

---

## Task 4: Fix renderRoom() — ordenación del rect procedural

**Files:**
- Modify: `frontend/src/app/features/simulator/game-world.component.ts:724-769`

### Contexto del bug

En `renderRoom()`, el bloque de suelo procedural (rect oscuro depth=0 + grilla depth=1) se añade **después** de `buildTiledLayers`. Phaser 3 dibuja objetos al mismo depth en orden de inserción. Los floor tiles (depth 0) se insertan dentro del try-block, y el rect oscuro (también depth 0) se insertaba después → el rect tapaba los tiles.

`renderWorld()` tiene el orden correcto (rect primero, luego tiles). Solo `renderRoom()` tenía el bug.

### Edición 1 — Insertar el bloque antes del try

- [ ] **Paso 4.1: Insertar rect+grilla antes del bloque `if (this.assetsLoaded)`**

En `frontend/src/app/features/simulator/game-world.component.ts`, localizar el fragmento exacto (alrededor de la línea 725) y hacer la edición:

**old_string:**
```typescript
    let tiledObjects: Phaser.Types.Tilemaps.TiledObject[] = [];

    if (this.assetsLoaded) {
      try {
        const tilemap = this.make.tilemap({ key: roomConfig.tiledMapKey });
```

**new_string:**
```typescript
    let tiledObjects: Phaser.Types.Tilemaps.TiledObject[] = [];

    // ── Dark procedural base — MUST come before buildTiledLayers ─────────────
    // Phaser draws same-depth objects in insertion order. Floor tiles (depth 0)
    // are inserted by buildTiledLayers next. This rect (also depth 0) is inserted
    // first, so floor tiles render ON TOP of it — not hidden underneath.
    this.add.rectangle(actualMapW / 2, actualMapH / 2, actualMapW - 40, actualMapH - 42, 0x131c28, 1).setDepth(0);
    const gfx = this.add.graphics().setDepth(1);
    gfx.lineStyle(1, 0x1c2d3e, 0.55);
    for (let gx = 16; gx < actualMapW; gx += 32) gfx.lineBetween(gx, 0, gx, actualMapH);
    for (let gy = 16; gy < actualMapH; gy += 32) gfx.lineBetween(0, gy, actualMapW, gy);
    // ─────────────────────────────────────────────────────────────────────────

    if (this.assetsLoaded) {
      try {
        const tilemap = this.make.tilemap({ key: roomConfig.tiledMapKey });
```

### Edición 2 — Eliminar el bloque de la posición antigua

- [ ] **Paso 4.2: Eliminar el bloque de la posición post-camera (alrededor de la línea 762)**

**old_string:**
```typescript
    // ── Dark procedural floor + subtle grid (mirrors renderWorld style) ───────
    // Floor tile layer uses GID 0 (transparent) everywhere, so this dark rect IS the floor.
    this.add.rectangle(actualMapW / 2, actualMapH / 2, actualMapW - 40, actualMapH - 42, 0x131c28, 1).setDepth(0);
    const gfx = this.add.graphics().setDepth(1);
    gfx.lineStyle(1, 0x1c2d3e, 0.55);
    for (let gx = 16; gx < actualMapW; gx += 32) gfx.lineBetween(gx, 0, gx, actualMapH);
    for (let gy = 16; gy < actualMapH; gy += 32) gfx.lineBetween(0, gy, actualMapW, gy);
    // ─────────────────────────────────────────────────────────────────────────

    // Room border
```

**new_string:**
```typescript
    // Room border
```

- [ ] **Paso 4.3: Verificar compilación TypeScript**

```bash
npx ng build --configuration development 2>&1 | tail -8
```

Resultado esperado: `Build at: ... - Hash: ... - Time: ...ms` sin errores. Si hay error `Cannot find name 'gfx'`: verificar que la Edición 1 incluyó `const gfx = ...` y que la Edición 2 no dejó una segunda declaración.

- [ ] **Paso 4.4: Commit del fix TypeScript**

```bash
git add frontend/src/app/features/simulator/game-world.component.ts
git commit -m "fix(game): move dark rect to before buildTiledLayers in renderRoom()

Same-depth objects in Phaser 3 render in insertion order. Floor tiles (depth 0)
must be inserted AFTER the dark background rect so they appear on top of it,
not hidden underneath. renderWorld() was already correct; only renderRoom() had
this ordering bug."
```

---

## Task 5: Build + Jest (CI local)

- [ ] **Paso 5.1: Suite de tests Jest completa**

```bash
cd frontend && npx jest --watchAll=false 2>&1 | tail -8
```

Resultado esperado: todos los tests pasan (≥ 92 según el estado actual). Si un test falla, es un bug introducido en Task 4 — revisar que solo se modificaron las líneas especificadas.

- [ ] **Paso 5.2: Build de producción limpio**

```bash
npx ng build --configuration development 2>&1 | tail -5
```

Sin errores. El JSON de mapas (assets estáticos) no afecta la compilación TS — este paso verifica que el fix TypeScript no introdujo errores de tipos.

---

## Task 6: Smoke test visual + ajuste de GIDs

Esta tarea verifica visualmente los 9 criterios de aceptación del spec. Los GIDs marcados `VALIDAR` en el generador pueden requerir ajuste si los tiles no son los correctos.

### Prerequisitos

Los servidores backend deben estar corriendo:
- Django: `python manage.py runserver` (puerto 8000)
- Spring: ya corriendo o no necesario para este smoke
- Frontend: `ng serve` (puerto 4200)

- [ ] **Paso 6.1: Abrir el simulador en el navegador**

Navegar a `http://localhost:4200/portal/simulador/1` (o el número de caso que cargue la comisaría). Iniciar la simulación hasta ver la sala de espera.

- [ ] **Paso 6.2: Verificar los 9 criterios de aceptación**

| # | Criterio | Acción |
|---|---|---|
| AC-1 | Build sin errores | Ya verificado en Paso 5.2 |
| AC-2 | Jest pasa | Ya verificado en Paso 5.1 |
| AC-3 | Suelo muestra tiles (no negro sólido) | Observar el suelo al cargar la sala |
| AC-4 | Ficheros y sillas visibles en zona norte | Mover cámara hacia el norte |
| AC-5 | Mostrador visible en primer plano | Inspeccionar zona central-sur |
| AC-6 | Mostrador tapa al jugador al cruzar fila ~21 | Mover personaje hacia el norte, cruzar mostrador |
| AC-7 | NPCs renderizan sobre el suelo correctamente | Buscar familiar-comisaria y colega-comisaria |
| AC-8 | Sin 404 de tilesets en consola | DevTools → Network → buscar "tilemap_packed.png" |
| AC-9 | Hueco al este del mostrador permite llegar a la puerta | Mover jugador por cols 37-58 hasta exit-to-consultorio |

- [ ] **Paso 6.3 (condicional): Ajuste de GIDs si un tile se ve incorrecto**

Si en AC-3 a AC-5 algún tile muestra el sprite equivocado:

1. Identificar qué GID es incorrecto (AC-3: floor, AC-4: props_back, AC-5: props_front)
2. Ajustar la constante correspondiente en `tools/map-generator/generate_comisaria_sala_espera.py`
3. Re-ejecutar: `python tools/map-generator/generate_comisaria_sala_espera.py`
4. Re-ejecutar los tests: `python tools/map-generator/test_generated_map.py`
5. Recargar el browser (`ng serve` detecta el cambio de asset automáticamente)
6. Verificar de nuevo los criterios afectados

**GIDs de referencia para depuración** (rpg-urban, firstgid=133, 27 cols):
- Fila 1 (GIDs 133-159): tilesets de suelo — si el suelo sale incorrecto, probar `_ru(1, 1)` a `_ru(1, 27)` incrementalmente
- Fila 4 (GIDs 214-240): mobiliario fila 4 — probar GIDs cercanos a `_ru(4, 7)` para sillas
- Fila 6-7 (GIDs 271-324): mostrador — probar variantes de `_ru(6,*)` y `_ru(7,*)`

- [ ] **Paso 6.4: Commit final (post ajuste de GIDs)**

Si se modificaron las constantes GID en el script:

```bash
git add tools/map-generator/generate_comisaria_sala_espera.py
git add frontend/src/assets/game/maps/comisaria-sala-espera.json
git commit -m "fix(maps): correct tile GIDs for comisaria floor/props after visual validation

Adjusted RU_* constants in generate_comisaria_sala_espera.py based on smoke
test: tiles now visually match intended design (floor tiles, waiting chairs,
filing cabinets, reception counter)."
```

---

## Self-Review

### Cobertura del spec

| Req del spec | Tarea |
|---|---|
| Script Python genera JSON completo | Task 2 |
| Tileset tiny-dungeon (firstgid=1) + rpg-urban (firstgid=133) | Task 2 paso 2.1 — `_TILESETS` |
| floor: chequerboard rows 1-31, cols 1-58 | Task 2 — `_paint_floor()` |
| walls: borde + hueco col59 filas 14-16 | Task 2 — `_paint_walls()` |
| props_back: ficheros NW/NE, sillas W/E, mesas centrales | Task 2 — `_paint_props_back()` |
| props_front: mostrador rows 20-21 + brazo L | Task 2 — `_paint_props_front()` |
| lighting/overlay: capas vacías | Task 2 — `_empty_layer()` |
| Objects: preservado exactamente | Task 2 — `_OBJECTS_LAYER` |
| Fix renderRoom() ordenación depth-0 | Task 4 |
| Tests estructurales del JSON | Task 1 |
| ng build + jest | Task 5 |
| Smoke 9 criterios de aceptación | Task 6 |

### Inconsistencias revisadas

- `const gfx`: declarada una sola vez (nueva posición en Task 4 Edición 1). La Edición 2 elimina la declaración original — no habrá `duplicate identifier`.
- `actualMapW/actualMapH` en el rect: el rect usa los valores iniciales (960, 528). El mapa es exactamente 60×33×16 = 960×528 → sin diferencia.
- `_ru()` helper vs `RU_*` constantes: todas las constantes se calculan en tiempo de importación del módulo; el helper no se llama en ningún path crítico de ejecución — no hay riesgo de recursión.
- Orden de capas en el JSON: `buildTiledLayers()` asigna depth por nombre (`tiledLayerDepth()`), no por posición en el array — el orden en `_MAP["layers"]` es irrelevante para el depth-sort.
